# Environment variables
.env
.env.local
.env.production

# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
jspm_packages/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# Runtime data (legacy - bot now uses Supabase)
# Keeping these in case of backup/export scripts
server-data.json
user-wallets-backup-*.json
current-round-backup-*.json
virtual-accounts.json
rps-games.json
used-tx-hashes.json
timestamp.json

# Football game data (legacy - bot now uses Supabase)
# Keeping in case data/ directory is created for exports
data/

# Runtime process files
pids
*.pid
*.seed
*.pid.lock

# Logs
logs/
*.log

# Coverage directory used by tools like istanbul
coverage/

# nyc test coverage
.nyc_output

# Migration scripts (one-time use, already completed)
# These files don't exist in current version - keeping pattern in case new ones are created
migrate-*.js
compare-*.js
*-migration-*.md

# Test files
test-*.js

# Alternative/utility scripts (not used by main bot)
# These files don't exist in current version - keeping pattern in case new ones are created
register-commands-*.js
!register-commands.js
delete-guild-*.js

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Backup files
*.backup
*.backup.*
*.bak
